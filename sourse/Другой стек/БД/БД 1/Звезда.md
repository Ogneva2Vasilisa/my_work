
```sql
CREATE TABLE dim_time (
    timestamp_id SERIAL PRIMARY KEY,
    timestamp TIMESTAMP,
    day INT,
    month INT,
    year INT,
    hour INT
);
```

	
```
INSERT INTO dim_time(timestamp, day, month, year, hour)
SELECT DISTINCT
    r.timestamp,
    EXTRACT(DAY FROM r.timestamp)::INT,
    EXTRACT(MONTH FROM r.timestamp)::INT,
    EXTRACT(YEAR FROM r.timestamp)::INT,
    EXTRACT(HOUR FROM r.timestamp)::INT 
FROM raw_logs r;
```




```sql
CREATE TABLE dim_source_ip (
    source_ip_id SERIAL PRIMARY KEY,
    source_ip VARCHAR(50)
);

INSERT INTO dim_source_ip(source_ip)
SELECT DISTINCT source_ip FROM raw_logs;
```

```sql
CREATE TABLE dim_dest_ip (
    dest_ip_id SERIAL PRIMARY KEY,
    dest_ip VARCHAR(50)
);

INSERT INTO dim_dest_ip(dest_ip)
SELECT DISTINCT dest_ip FROM raw_logs;
```

```sql
CREATE TABLE dim_protocol (
    protocol_id SERIAL PRIMARY KEY,
    protocol VARCHAR(20)
);

INSERT INTO dim_protocol(protocol)
SELECT DISTINCT protocol FROM raw_logs;
```

```sql
CREATE TABLE dim_action (
    action_id SERIAL PRIMARY KEY,
    action VARCHAR(50)
);

INSERT INTO dim_action(action)
SELECT DISTINCT action FROM raw_logs;
```

```sql
CREATE TABLE dim_threat_label (
    threat_label_id SERIAL PRIMARY KEY,
    threat_label VARCHAR(50)
);

INSERT INTO dim_threat_label(threat_label)
SELECT DISTINCT threat_label FROM raw_logs;
```

```sql
CREATE TABLE dim_log_type (
    log_type_id SERIAL PRIMARY KEY,
    log_type VARCHAR(50)
);

INSERT INTO dim_log_type(log_type)
SELECT DISTINCT log_type FROM raw_logs;
```

---

## **факт-таблицу**

```sql
CREATE TABLE fact_network_events (
    event_id SERIAL PRIMARY KEY,
    timestamp_id INT REFERENCES dim_time(timestamp_id),
    source_ip_id INT REFERENCES dim_source_ip(source_ip_id),
    dest_ip_id INT REFERENCES dim_dest_ip(dest_ip_id),
    protocol_id INT REFERENCES dim_protocol(protocol_id),
    action_id INT REFERENCES dim_action(action_id),
    threat_label_id INT REFERENCES dim_threat_label(threat_label_id),
    log_type_id INT REFERENCES dim_log_type(log_type_id),
    bytes_transferred BIGINT
);
```

- Сначала для каждой строки `raw_logs` ищем соответствующие ID из таблиц измерений.
    
- Потом вставляем факты + ссылки на измерения.
    
```sql
INSERT INTO fact_network_events(
    timestamp_id, source_ip_id, dest_ip_id, protocol_id,
    action_id, threat_label_id, log_type_id, bytes_transferred
)
SELECT 
    t.timestamp_id,
    s.source_ip_id,
    d.dest_ip_id,
    p.protocol_id,
    a.action_id,
    th.threat_label_id,
    l.log_type_id,
    r.bytes_transferred 
FROM raw_logs r 
JOIN dim_time t ON t.timestamp = r.timestamp 
JOIN dim_source_ip s ON s.source_ip = r.source_ip 
JOIN dim_dest_ip d ON d.dest_ip = r.dest_ip 
JOIN dim_protocol p ON p.protocol = r.protocol 
JOIN dim_action a ON a.action = r.action 
JOIN dim_threat_label th ON th.threat_label = r.threat_label 
JOIN dim_log_type l ON l.log_type = r.log_type; 

```


